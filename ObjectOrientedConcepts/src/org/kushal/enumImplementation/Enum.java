package org.kushal.enumImplementation;

public enum Enum {
	COREJAVA(1), COLLECTIONS(2), GENERICS(3), JASPANDSERVLET(4), MULTITHREADING(5);

	private int value;

	Enum(int value) {
		this.value = value;
	}
	
	
}
